[defaults]
inventory = inventory.ini
host_key_checking = False
vault_password_file = ~/.ansible/vault/main_pass
stdout_callback = default

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
