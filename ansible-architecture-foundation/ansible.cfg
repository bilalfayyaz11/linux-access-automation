[defaults]
inventory = ./inventory
host_key_checking = False
remote_user = centos
stdout_callback = default

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
